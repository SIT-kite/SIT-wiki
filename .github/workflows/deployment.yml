# This workflow will install Python dependencies, build wiki pages and deploy.
# For more information see:
# https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Publish Wiki

on:
  push:         { branches: [ master ] }
  pull_request: { branches: [ master ] }

jobs:

  build:
    runs-on: ubuntu-latest
    strategy: { matrix: { python-version: [ "3.11" ] } }

    steps:

    - name: Checkout repo
      uses: actions/checkout@v2
      # https://timvink.github.io/mkdocs-git-revision-date-localized-plugin/#note-when-using-build-environments
      with: { fetch-depth: 0 }

    - name: Setup Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
        cache: pip

    - name: Install requirements
      run: |
        pip install -U pip
        pip install -r requirements.txt

    - name: Build wiki
      run: mkdocs build

    - name: Copy files via SSH
      uses: appleboy/scp-action@v0.1.3
      with:
        host:     ${{ secrets.SERVER_HOST }}
        port:     ${{ secrets.PORT }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_SECRET }}
        source: ./site/*
        target: /var/kite/SIT-wiki/site/
        strip_components: 1

    # - name: refresh Aliyun CDN
    #   uses: weirui88888/aliyun-cdn-sdk@v1.0.0
    #   with:
    #     accessKeyId: ${{ secrets.ALIYUN_ACCESS_KEY }}
    #     accessKeySecret: ${{ secrets.ALIYUN_SECRET }}
    #     parameters: '{"Action": "RefreshDcdnObjectCaches", "ObjectPath": "https://kite.sunnysab.cn/wiki/", "ObjectType": "Directory"}'

  notification:
    runs-on: ubuntu-latest
    needs: build
    if: always()
    steps:

    - uses: technote-space/workflow-conclusion-action@v2

    - uses: benjlevesque/short-sha@v2.1
      id: short-sha
      with: { length: 7 }

    - name: 🎯 Telegram Notification
      if: env.WORKFLOW_CONCLUSION == 'success'
      uses: appleboy/telegram-action@v0.1.1
      with:
        to: ${{ secrets.CHAT_ID }}
        token: ${{ secrets.BOT_TOKEN }}
        format: markdown
        message: |
          🎉 [Wiki](https://github.com/SIT-kite/SIT-wiki)  Build  *${{ env.WORKFLOW_CONCLUSION }}*

          Author: *${{ github.actor }}*

          Message: *${{ github.event.head_commit.message }}*

          Commit: [${{ env.SHA }}](https://github.com/${{ github.repository }}/commit/${{ env.SHA }})

    - name: 🎯 Telegram Notification
      if: env.WORKFLOW_CONCLUSION != 'success'
      uses: appleboy/telegram-action@v0.1.1
      with:
        to: ${{ secrets.CHAT_ID }}
        token: ${{ secrets.BOT_TOKEN }}
        format: markdown
        message: |
          🔴 [Wiki](https://github.com/SIT-kite/SIT-wiki)  Build  *${{ env.WORKFLOW_CONCLUSION }}*

          Author: *${{ github.actor }}*

          Message: *${{ github.event.head_commit.message }}*

          Commit: [${{ env.SHA }}](https://github.com/${{ github.repository }}/commit/${{ env.SHA }})
